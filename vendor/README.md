# Bundled dependencies

Small Python parser packages support offline builds and self-contained client scripts. `manifest.json` records their versions and checksums. Each ZIP contains only runtime code and its original MIT license.

`notices/` contains license notices included with downloaded programs. Application binaries, GeoIP, source archives and hosting operations belong to the separate resource project.

Update these files as a reviewed bundle; do not edit upstream code or remove notices.
